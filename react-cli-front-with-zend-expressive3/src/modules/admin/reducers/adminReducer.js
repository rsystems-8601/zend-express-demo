// @flow
import { adminActions } from '../constants/adminConstants';


export const initialState = {
};

const adminReducer = (state: Object = initialState, action: Object) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default adminReducer;

